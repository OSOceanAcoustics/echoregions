version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
    nodejs: "22"
  jobs:
    pre_build:
      - "uv run jupyter-book build --html"

formats: []

python:
  install:
    - method: uv
      command: sync
      path: .
      extra_requirements:
        - docs
